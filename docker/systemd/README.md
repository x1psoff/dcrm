# Systemd Автозапуск DCRM

Этот каталог содержит файлы для настройки автоматического запуска всех сервисов DCRM при загрузке системы.

## 🚀 Быстрая установка

```bash
cd /home/qwe/dcrm/docker/systemd
sudo ./install.sh
```

После установки все сервисы (веб-приложение, бот, бэкапы) будут автоматически запускаться при загрузке системы.

## 📦 Что включено

Служба автоматически запускает следующие сервисы:

- **web** - Django веб-приложение (порт 8000)
- **proxy** - Traefik reverse proxy
- **bot** - Telegram бот
- **backup** - Ежедневные бэкапы (23:05 UTC)
- **backup_orders** - Частые бэкапы заказов (каждые 15 минут)
- **selenium** - Selenium Chrome для автоматизации
- **ufaloft** - Watcher для синхронизации

## 📋 Управление службой

### Основные команды

```bash
# Запустить службу
sudo systemctl start dcrm

# Остановить службу
sudo systemctl stop dcrm

# Перезапустить службу
sudo systemctl restart dcrm

# Статус службы
sudo systemctl status dcrm

# Просмотр логов
sudo journalctl -u dcrm -f

# Просмотр последних 100 строк логов
sudo journalctl -u dcrm -n 100
```

### Автозапуск

```bash
# Включить автозапуск (уже включен после install.sh)
sudo systemctl enable dcrm

# Отключить автозапуск
sudo systemctl disable dcrm
```

### Перезагрузка после изменений в коде

```bash
# Пересобрать и перезапустить контейнеры
sudo systemctl reload dcrm

# Или остановить службу, обновить и запустить
sudo systemctl stop dcrm
cd /home/qwe/dcrm
docker compose --profile bot --profile backup --profile backup_orders up -d --build
```

## 🗑️ Удаление службы

```bash
cd /home/qwe/dcrm/docker/systemd
sudo ./uninstall.sh
```

## 🔍 Проверка работы

После установки проверьте статус:

```bash
# Статус systemd службы
sudo systemctl status dcrm

# Статус Docker контейнеров
cd /home/qwe/dcrm
docker compose ps
```

## 📝 Структура файлов

```
docker/systemd/
├── dcrm.service      # Файл systemd службы
├── install.sh        # Скрипт установки
├── uninstall.sh      # Скрипт удаления
└── README.md         # Эта инструкция
```

## ⚙️ Технические детали

- **Тип службы**: oneshot с RemainAfterExit
- **Зависимости**: docker.service, network-online.target
- **Пользователь**: qwe
- **WorkingDirectory**: /home/qwe/dcrm
- **Политика перезапуска**: on-failure (с задержкой 10 секунд)

## 🔧 Настройка переменных окружения

Все настройки берутся из `.env` файла в `/home/qwe/dcrm/.env`. Для изменения параметров:

1. Отредактируйте `.env` файл
2. Перезапустите службу: `sudo systemctl restart dcrm`

## 📊 Мониторинг

### Просмотр логов всех контейнеров

```bash
cd /home/qwe/dcrm

# Все логи
docker compose logs -f

# Только веб-приложение
docker compose logs -f web

# Только бот
docker compose logs -f bot

# Только бэкапы
docker compose logs -f backup backup_orders
```

### Проверка healthcheck

```bash
# Статус здоровья контейнеров
docker compose ps

# Детальная информация о контейнере
docker inspect dcrm-web-1 | grep -A 10 Health
```

## 🆘 Решение проблем

### Служба не запускается

```bash
# Проверить логи systemd
sudo journalctl -u dcrm -n 50

# Проверить статус Docker
sudo systemctl status docker

# Проверить конфигурацию docker-compose
cd /home/qwe/dcrm
docker compose config
```

### Контейнеры не стартуют

```bash
# Проверить логи контейнеров
cd /home/qwe/dcrm
docker compose logs

# Пересобрать контейнеры
docker compose --profile bot --profile backup --profile backup_orders up -d --build

# Проверить права на файлы
ls -la db.sqlite3 media/
```

### Перезапуск после обновления системы

После обновления системы или перезагрузки сервера служба запустится автоматически. Если нужно запустить вручную:

```bash
sudo systemctl start dcrm
```

## 📚 Дополнительная информация

- Docker Compose документация: https://docs.docker.com/compose/
- Systemd документация: https://www.freedesktop.org/software/systemd/man/systemd.service.html
- Контекст проекта: см. `/home/qwe/dcrm/AI_CONTEXT.md`

