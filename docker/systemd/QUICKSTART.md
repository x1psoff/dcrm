# 🚀 Автозапуск DCRM - Краткая инструкция

## Быстрый старт

```bash
cd /home/qwe/dcrm/docker/systemd
sudo ./install.sh
```

Готово! Все сервисы будут автоматически запускаться при загрузке системы.

## Что запускается автоматически

- ✅ **Web** - Django приложение (порт 8000)
- ✅ **Proxy** - Traefik reverse proxy
- ✅ **Bot** - Telegram бот для уведомлений
- ✅ **Backup** - Ежедневные бэкапы (23:05 UTC)
- ✅ **Backup Orders** - Частые бэкапы заказов (каждые 15 мин)
- ✅ **Selenium** - Браузер для автоматизации
- ✅ **Ufaloft** - Синхронизация данных

## Основные команды

```bash
# Статус службы
sudo systemctl status dcrm

# Перезапуск (после изменений в коде)
sudo systemctl restart dcrm

# Остановка
sudo systemctl stop dcrm

# Запуск
sudo systemctl start dcrm

# Логи в реальном времени
sudo journalctl -u dcrm -f

# Последние 100 строк логов
sudo journalctl -u dcrm -n 100
```

## Проверка работы

```bash
# Статус systemd службы
sudo systemctl status dcrm

# Статус Docker контейнеров
cd /home/qwe/dcrm
docker compose ps

# Логи веб-приложения
docker compose logs web -f
```

## После изменения кода

Если вы изменили код приложения, пересоберите контейнеры:

```bash
sudo systemctl stop dcrm
cd /home/qwe/dcrm
docker compose --profile bot --profile backup --profile backup_orders up -d --build
# или просто
sudo systemctl restart dcrm
```

## Удаление автозапуска

```bash
cd /home/qwe/dcrm/docker/systemd
sudo ./uninstall.sh
```

## Полная документация

См. файл `README.md` в этой же директории.

